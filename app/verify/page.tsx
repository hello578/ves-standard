import type { Metadata } from "next"
import { VerifierDemo } from "../components/verifier-demo"
import { verificationDisclaimer } from "@/lib/site"

export const metadata: Metadata = { title: "Verify", description: "A transparent browser-only structural validation demonstration for VES 1.2.", alternates: { canonical: "/verify" } }
export default function VerifyPage(){return <main><section className="page-hero verify-hero"><div className="container"><p className="eyebrow">Transparent demonstration</p><h1>Inspect a VES package.</h1><p>Validate a fictional Decision Envelope against the published VES 1.2 JSON Schema. The default package demonstrates a Qualified result with explicit limitations.</p></div></section><section className="verify-section"><div className="container"><VerifierDemo/><div className="verification-disclaimer"><strong>Verification boundary</strong><p>{verificationDisclaimer}</p><p>Structural validation is not VES verification by itself. A PDF alone or a JSON file alone is not automatically verified. The submitted JSON Decision Envelope is the structural unit assessed by this demonstration; source artefacts are not independently verified here.</p><p>Runtime-control receipts, model-gateway logs, signed action records and policy-enforcement records may form part of a VES Decision Envelope. They do not, by themselves, establish the full business decision-state unless linked to applicable evidence, business authority, human judgement and organisational commitment.</p><p>Governance evidence may be represented as an Evidence Object, but it does not by itself establish the basis of a specific consequential decision.</p></div></div></section></main>}
