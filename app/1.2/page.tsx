import type { Metadata } from "next"
import StandardPage from "../standard/page"

export const metadata: Metadata = { title: "VES 1.2", description: "The current published VES 1.2 standard.", alternates: { canonical: "/1.2" } }

export default StandardPage
